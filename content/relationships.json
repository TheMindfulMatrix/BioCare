{
  "schemaVersion": "1.0",
  "description": "Canonical navigation relationships. Source product_ids indicate ingredient/topic context only, never proof of a finished product's safety or effectiveness. Evidence type and limitations remain visible.",
  "rules": {
    "productDepartment": "catalog.products[].intent -> discovery.departments[].intentId",
    "departmentEducation": "discovery.departments[].articleSlugs",
    "productEvidence": "resources/public-sources.json records[].product_ids",
    "departmentEvidence": "resources/public-sources.json records[].department_ids",
    "relatedProducts": "same catalog intent, canonical catalog order"
  }
}
